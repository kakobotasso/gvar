<h2>Resultado da pesquisa</h2>
<table cellspadding="0" cellspacing="0" width="100%">
  <tr border="0">
  <th width="20%"><span>Nome</span></th>
  <th width="20%"><span>CPF</span></th>
  <th width="20%"><span>RG</span></th>
  <th width="10%">Estado</th>
  <th width="20%">Cidade</th>
  <th width="5%">Ativo</th>
  <th width="20%"></th>
  </tr>

  <% providers.each do |provider| %>
    <tr onclick="location.href = '<%= edit_provider_path(provider) %>'">
      <td><span><%= provider.name %></span></td>
      <td><span><%= provider.cpf %></span></td>
      <td><span><%= provider.rg %></span></td>
      <td><span><%= provider.estado %></span></td>
      <td><span><%= provider.address.city %></span></td>
      <td><span><%= provider.is_active %></span></td>
      <td><span class="centralizaBts"><%= link_to 'Deletar', provider, method: :delete, data: { confirm: 'Deseja realmente exlcuir este providere do sistema?' }, class: "btVoltarExcluirForm" %></span></td>
    </tr>
  <% end %>
</table>

